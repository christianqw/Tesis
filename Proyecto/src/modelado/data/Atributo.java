/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package modelado.data;
import java.util.Collection;
import java.util.Set;
/**
 *
 * @author Chris
 */
public class Atributo {
    /*
    CLASE QUE NO ES UTILIZADA EN REALIDAD. 
    ELIMINADA CUANDO SE HIZO REFACTORIZACION 
    
    private Opciones _opciones;
    private int _value;

    public Atributo(Opciones opciones) {
        this._opciones = opciones;
    }

    public Atributo(Opciones opciones, String name) {
        this._opciones = opciones;
        this._value = _opciones.getValue(name);
    }

    public Atributo(Opciones opciones, int value) {
        this._opciones = opciones;
        this._value = value;
    }
    
    public int getValue() {
        return _value;
    }
    
    public String getNameValue(){
        return _opciones.getNameValue(this._value);
    }

    public boolean setValue(String name) {
        if (this._opciones.contains(name)){
            this._value = _opciones.getValue(name);
            return true;
        }
        else return false; 
    }
    public boolean setValue(int value) {
        if (this._opciones.contains(value)){
            this._value = value;
            return true;
        }
        else return false; 
    }
    */
    
}
